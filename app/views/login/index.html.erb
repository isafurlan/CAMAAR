<!DOCTYPE html>
<html>
<head>
  <link href="https://fonts.googleapis.com/css?family=Roboto&display=swap" rel="stylesheet" />
  <link href="app/assets/stylesheets/login.css" rel="stylesheet" />
  <title>Login</title>
</head>
<body>
  <div class="Fundo-branco">
    <div class="Fundo-roxo">
      <!-- Lado esquerdo: login -->
      <div class="Caixa-login">
        <span class="login">LOGIN</span>

        <%= form_with url: '/login', method: :post, local: true do %>
          <div class="email">
            <%= label_tag :email, "Email", class: "email" %><br>
            <%= email_field_tag :email, nil, placeholder: "aluno@aluno.unb.br", class: "input" %>
          </div>

          <div class="senha">
            <%= label_tag :senha, "Senha", class: "senha" %><br>
            <%= password_field_tag :password, nil, placeholder: "Password", class: "input" %>
          </div>

          <div>
            <%= submit_tag "Entrar", class: "entrar" %>
          </div>
        <% end %>
      </div>

      <div class="Caixa-bv">
        <span class="bem-vindo">Bem vindo<br>
          ao<br>
          Camaar
        </span>
      </div>
    </div>
  </div>
</body>
</html>
